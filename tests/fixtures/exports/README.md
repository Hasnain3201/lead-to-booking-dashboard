# Sample export fixtures

These CSV files are synthetic examples derived from the committed demo workspace.

- `booking-outcomes.csv` contains the demo workspace's cancelled and no-show booking export.
- `import-issues.csv` shows the validation report for the deliberately broken bookings file in `data/invalid/`.

Use them to inspect and manually verify the dashboard's CSV export formats. They contain no real customer data.
